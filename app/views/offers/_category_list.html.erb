<% ct =  defined?(category) ? category : Restaurant.where(user_id: current_user.id).first.id %>
<%= label_tag :category %><br />
<%= select_tag "offer[category_id]", options_for_select(Restaurant.find(ct).categories.pluck(:name)), id: 'offer_category_id', remote: true%>



<script type="text/javascript">
    $("#offer_cate_id").on("click", function()
    { 
      console.log("haqKZ");
      $.ajax({
        url: 'change_foodlist.js',
        method: 'GET',
        data: 
        {
          food: $(this).val()

        }
      });
    });

</script>