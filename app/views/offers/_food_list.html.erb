<% ct =  defined?(food) ? food : Category.first.id %>
<%= label_tag :food_name %>

<%= select_tag "offer[food_id]", options_for_select(Category.find(ct).food_items.pluck(:name)), id: 'offer_category_id'%><br />

